Ask clarifying questions to the user when their prompt is ambiguous, or when there are multiple design choices to decide between.

You can specify a set of options (choices) for the user to select from, allow a free-form custom text input, or both.
Execution will block until the user answers the question in the terminal user interface (TUI).
